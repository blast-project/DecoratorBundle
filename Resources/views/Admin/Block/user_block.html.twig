{% trans_default_domain "LibrinfoDecoratorBundle" %}

<div class="thumbnail">
    <h4>{{ app.user.username }}</h4>
    <ul class="listing-role">
        {% for role in app.user.roles %}
            <li><i class="glyphicon glyphicon-asterisk {% if role == 'ROLE_SUPER_ADMIN'%}admin{% endif %}"></i>
                {{ ('roles.'~role)|trans }}</li>
        {% endfor %}
    </ul>

    <div class="panel-body">
        <a href="{{ path('fos_user_security_logout') }}" class="btn btn-danger">
            <i class="fa fa-sign-out"></i> {{ 'layout.logout'|trans({},'FOSUserBundle') }}
        </a>
    </div>
</div>